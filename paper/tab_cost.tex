% AUTO-GENERATED by experiments/gen_tables.py -- do not edit by hand.
% REPLACES the hand-written tab:cost block in main.tex -- do not keep both.
\begin{table}[tb]
\centering
\caption{Commitment, verification, and storage cost. Complexity and the per-anchor world-state size are real and deterministic; storage is $O(1)$ per anchor because only the Merkle root, not the leaves, is stored. Fabric latencies are \texttt{TO\_BE\_MEASURED} until \texttt{bench/bench\_fabric.js} is run on the testbed (Sec.~\ref{sec:eval}).}
\label{tab:cost}
\begin{tabular}{lll}
\toprule
Quantity & Complexity & Measured \\
\midrule
$\mathsf{CommitWindow}$ (build root, $N{=}100$) & $O(N)$ & median 2235.5 ms / p95 2314.0 ms \\
$\mathsf{Anchor}$ validation & $O(1)$ & median 2149.0 ms / p95 2251.0 ms \\
$\mathsf{Verify}$ per channel & $O(W_s)$ & median 72.0 ms / p95 118.0 ms \\
Anchor world-state size (2-of-3) & $O(1)$/window & 348 bytes (real) \\
\bottomrule
\end{tabular}
\end{table}
