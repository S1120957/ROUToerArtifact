% AUTO-GENERATED by experiments/gen_tables.py -- do not edit by hand.
% Requires \usepackage{amssymb} in the preamble (for \checkmark).
% REPLACES the hand-written tab:adv block in main.tex -- do not keep both.
\begin{table}[tb]
\centering
\caption{Adversarial coordinator vs.\ verifier. The detection column is produced by \texttt{run\_security\_battery.py}, a deterministic reference simulator, and is reproducible with \texttt{make security}; it is not a Fabric-timing result.}
\label{tab:adv}
\begin{tabular}{lll}
\toprule
Adversarial behaviour & Detected & Mechanism \\
\midrule
Drop event within a window & \checkmark & Thm.~\ref{thm:within} (root mismatch) \\
Drop an interior window & \checkmark & Thm.~\ref{thm:whole} (commit-time reject) \\
Drop the tail (truncation) & \checkmark & freshness deadline (stall) \\
Reorder within a window & \checkmark & Thm.~\ref{thm:reorder} \\
Insert a fabricated event & \checkmark & Thm.~\ref{thm:reorder} \\
Replay an old window & \checkmark & Thm.~\ref{thm:replay} \\
\bottomrule
\end{tabular}
\par\smallskip
{\footnotesize Sanity: the honest baseline is \emph{accepted} (not flagged), confirming the verifier does not reject indiscriminately.}
\end{table}
